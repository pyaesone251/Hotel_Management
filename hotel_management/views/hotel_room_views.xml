<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Room Tree View -->
    <record id="hotel_room_view_list" model="ir.ui.view">
        <field name="name">hotel.room.list</field>
        <field name="model">hotel.room</field>
        <field name="arch" type="xml">
            <list decoration-success="state == 'available'"
                  decoration-danger="state == 'occupied'"
                  decoration-warning="state == 'maintenance'"
                  decoration-info="state == 'cleaning'">
                <field name="name"/>
                <field name="room_type_id"/>
                <field name="floor"/>
                <field name="price_per_night" widget="monetary"/>
                <field name="max_adults"/>
                <field name="max_children"/>
                <field name="state" widget="badge"
                       decoration-success="state == 'available'"
                       decoration-danger="state == 'occupied'"
                       decoration-warning="state == 'maintenance'"
                       decoration-info="state == 'cleaning'"/>
            </list>
        </field>
    </record>

    <!-- Room Form View -->
    <record id="hotel_room_view_form" model="ir.ui.view">
        <field name="name">hotel.room.form</field>
        <field name="model">hotel.room</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_available" string="Set Available"
                            type="object" class="btn-primary"
                            invisible="state == 'available'"/>
                    <button name="action_maintenance" string="Maintenance"
                            type="object"
                            invisible="state == 'maintenance'"/>
                    <button name="action_cleaning" string="Cleaning"
                            type="object"
                            invisible="state == 'cleaning'"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="available,occupied,cleaning"/>
                </header>
                <sheet>
                    <field name="image" widget="image" class="oe_avatar"/>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="e.g. 101"/>
                        </h1>
                    </div>
                    <group>
                        <group string="Room Details">
                            <field name="room_type_id"/>
                            <field name="floor"/>
                            <field name="price_per_night" widget="monetary"/>
                            <field name="active"/>
                        </group>
                        <group string="Capacity">
                            <field name="max_adults"/>
                            <field name="max_children"/>
                            <field name="current_guest_id"/>
                            <field name="current_reservation_id"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Amenities" name="amenities">
                            <field name="amenities" placeholder="Room-specific amenities..."/>
                        </page>
                        <page string="Description" name="description">
                            <field name="description"/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- Room Kanban View -->
    <record id="hotel_room_view_kanban" model="ir.ui.view">
        <field name="name">hotel.room.kanban</field>
        <field name="model">hotel.room</field>
        <field name="arch" type="xml">
            <kanban default_group_by="state" class="o_kanban_small_column">
                <field name="name"/>
                <field name="room_type_id"/>
                <field name="floor"/>
                <field name="state"/>
                <field name="price_per_night"/>
                <field name="current_guest_id"/>
                <field name="color"/>
                <templates>
                    <t t-name="card" class="flex-row">
                        <aside>
                            <field name="image" widget="image"
                                   options="{'preview_image': 'image', 'size': [90, 90]}"/>
                        </aside>
                        <main>
                            <field name="name" class="fw-bolder fs-5"/>
                            <field name="room_type_id"/>
                            <div>Floor: <field name="floor"/></div>
                            <div>
                                <field name="price_per_night" widget="monetary"/> / night
                            </div>
                            <div t-if="record.current_guest_id.value">
                                <i class="fa fa-user"/> <field name="current_guest_id"/>
                            </div>
                        </main>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Room Search View -->
    <record id="hotel_room_view_search" model="ir.ui.view">
        <field name="name">hotel.room.search</field>
        <field name="model">hotel.room</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="room_type_id"/>
                <field name="floor"/>
                <separator/>
                <filter string="Available" name="available"
                        domain="[('state', '=', 'available')]"/>
                <filter string="Occupied" name="occupied"
                        domain="[('state', '=', 'occupied')]"/>
                <filter string="Maintenance" name="maintenance"
                        domain="[('state', '=', 'maintenance')]"/>
                <filter string="Cleaning" name="cleaning"
                        domain="[('state', '=', 'cleaning')]"/>
                <separator/>
                <group expand="0" string="Group By">
                    <filter string="Room Type" name="group_type"
                            context="{'group_by': 'room_type_id'}"/>
                    <filter string="Floor" name="group_floor"
                            context="{'group_by': 'floor'}"/>
                    <filter string="Status" name="group_state"
                            context="{'group_by': 'state'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Room Action -->
    <record id="hotel_room_action" model="ir.actions.act_window">
        <field name="name">Rooms</field>
        <field name="res_model">hotel.room</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="context">{'search_default_available': 0}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Add your first Hotel Room
            </p>
            <p>
                Create and manage individual hotel rooms.
            </p>
        </field>
    </record>

</odoo>
