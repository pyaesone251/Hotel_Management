<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_hotel_room_form" model="ir.ui.view">
        <field name="name">hotel.room.form</field>
        <field name="model">hotel.room</field>
        <field name="arch" type="xml">
            <form string="Hotel Room">
                <header>
                    <button name="action_available" string="Available"
                            type="object" class="btn-outline-success"
                            invisible="state == 'available'"/>
                    <button name="action_maintenance" string="Maintenance"
                            type="object" class="btn-outline-danger"
                            invisible="state == 'maintenance'" />
                    <field name="state" widget="statusbar"
                            statusbar_visible="available,occupied,maintenance"/>
                </header>
            </form>
        </field>
    </record>

    <record id="action_hotel_room_view" model="ir.actions.act_window">
        <field name="name">Hotel Room</field>
        <field name="res_model">hotel.room</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>